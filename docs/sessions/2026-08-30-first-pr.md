# 2026-08-30 — первый PR

## Context

Скелет плагина и контракт репо жили в working tree на `main`. Флоу требует
ветку и PR.

## What

Ветка `feat/plugin-skeleton`: CMake/JUCE N-in/N-out, тесты, AGENTS, планы,
handoff. Один PR в `main`, squash. GCC-PHAT не включён.

## Why

Два PR (скелет vs доки) отложили: на пустом `main` это один фундамент, без
которого второму агенту не с чего продолжать. Дробить после появления `main`
с кодом.

Прямой пуш в `main` отвергнут — первый же PR должен сесть в тот же флоу,
что и все следующие.

## Status

status.md обновлён: да. Ветка: `feat/plugin-skeleton`.

## Next

Merge PR 1, затем `feat/gcc-phat`.

## Open

Require PR on GitHub `main` ещё не включён.

# 2026-08-30 — Makefile

## Context

Пробный билд руками через cmake presets неудобен; нужны форматы под ОС
одной командой.

## What

Корневой `Makefile`: `make` = release tests + plugin targets этой платформы.
macOS: VST3 AU Standalone. Иначе VST3 + Standalone. `make run` / `where` / `clean`.

Release-сборка уже прошла (тесты зелёные, VST3/AU установлены в
`~/Library/Audio/Plug-Ins/`).

## Why

Не отдельный `scripts/build.sh`: `make` короче и покрывает цели. CMake остаётся
системой сборки; Makefile только пресеты и список таргетов.

## Status

status.md обновлён: да. Ветка: `feat/fractional-delay`.

## Next

Merge #3, проверка в Reaper, PR 4.

## Open

Нет.

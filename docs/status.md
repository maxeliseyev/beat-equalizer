# Status

Updated: 2026-09-01
Stage: 1 (static alignment), рамка этапов 2–4 пересмотрена
Plan step: PR 8 пройден на реальном ките; следующий — PR 9, монитор-микс стенда
Branch: `docs/editor-in-standalone`
PR: https://github.com/maxeliseyev/beat-equalizer/pull/17
Blockers: none

## Done

- PR 1–8 плана: последний — прогон реального кита через стенд.
- Репо-PR 1–16 в `main`, последний (#16) — клип стенда без замка, транспорт,
  диагностика устройства.
- На ветке: только документы. Решение «этапы 2–3 в standalone-редакторе, ARA —
  этап 4, канал доставки», границы редактора, расчёт по объёму материала.

## Now

Прогон на ките дал два вывода. Первый: не хватает панорамы — стенд отдаёт канал
клипа в одноимённый выход устройства, и на стерео-карте кит звучит как
«нечётные слева, чётные справа», дорожек дальше второй не слышно. Второй:
монтаж имеет смысл делать в своём приложении, а не через ARA.

Кода в этой ветке нет, тесты не трогались.

## Next

PR 9 плана — монитор-микс стенда: панорама и уровень на канал, сумма всех
каналов в 1-2. Работает только при загруженном материале (внутри хоста N-out
passthrough не трогаем), в `Export aligned` не попадает. Панорама по умолчанию
центр.

Дальше — этап 2 по `decisions/editor-in-standalone-ara-is-delivery.md`: сначала
библиотека документа рядом с `beat_dsp`, потом события и по-ударное
выравнивание по `timing-design-recommendations.md`.

## Resume

1. `git fetch && git checkout main && git pull`
2. `make test && make test-gui && make standalone && make run`
3. В Standalone: Audio…, Load files… — кит слышно только на каналах 1-2
   устройства; это и чинит PR 9.

## Open

- Клип целиком в памяти: 16 дорожек по 6 минут на 96 кГц ≈ 2.2 ГБ. Решение
  (memory-mapped источники, пик-пирамида) описано в плане, секция 8, не сделано.
- Проигрывание ограничено каналами устройства; анализ, картинка и экспорт — нет.
- Файлы обязаны быть экспортированы от одного нуля сессии.
- Ротатор не участвует в отрисовке трасс (только в звуке и экспорте).
- Solo/Mute — мониторинг: в `Export aligned` не попадают.
- Сетка на стенде считается от начала клипа: своей позиции в такте у файла нет.
- Протокол ожидаемых задержек по итогам прогона в `docs/` ещё не записан.
- GitHub Require PR on `main`.

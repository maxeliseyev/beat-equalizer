# Status

Updated: 2026-09-01
Stage: 1 (static alignment)
Plan step: PR 9 — стенд: монитор-микс, длинное окно, обзор, компактная строка
Branch: `feat/compact-channel-row`
PR: —
Blockers: none

## Done

- PR 1–8 плана: последний — прогон реального кита через стенд.
- Репо-PR 1–16 в `main`. Ветка замыкает стопку #17 → #18 → #19.
- На ветке: то же плюс компактная строка канала — окно 1180 вместо 1560,
  колонка Role убрана. 0.11.0. Лежит поверх #22.

## Now

Ветка зелёная: `make test` 67 кейсов, `make test-gui` 51 кейс.

Стенд после прогона на ките: кит сводится в выходы 1-2 с панорамой и уровнем
(#18), окно Time доросло до 2 с и прореживается при отрисовке, а транспорт
показывает всю партию огибающей — с курсором, окном строк и временем
«прошло / всего / осталось».

## Next

Этап 2 по `decisions/editor-in-standalone-ara-is-delivery.md`: сначала
библиотека документа рядом с `beat_dsp` (источники, события, поле задержек),
потом события и по-ударное выравнивание по `timing-design-recommendations.md`.

Отдельно: записать в `docs/` протокол ожидаемых задержек по итогам прогона.
Возврат задержки комнаты (`r` на канал) и сегментация удара делаются в этапе 2,
не раньше. Ступени 1–3 лестницы сегментации не требуют ни данных, ни обучения.

## Resume

1. `git fetch && git checkout feat/bench-monitor-mix && git pull`
2. `make test && make test-gui && make standalone && make run`
3. В Standalone: Load files… — кит слышен целиком в обоих выходах; Pan уводит
   канал в сторону, Level подстраивает баланс. Time 2000 мс — окно во всю фразу;
   клик по полосе обзора перематывает.

## Open

- Клип целиком в памяти: 16 дорожек по 6 минут на 96 кГц ≈ 2.2 ГБ. Решение
  (memory-mapped источники, пик-пирамида) описано в плане, секция 8, не сделано.
- Мониторинг идёт только в выходы 1-2: кит на восемь выходов карты не разводится.
- В хосте окно Time упирается в секунду: столько держит кольцо живого входа.
- На длинном окне колонка Corr считается по прореженным точкам, то есть по
  огибающей; форму волны смотреть на коротком окне.
- Файлы обязаны быть экспортированы от одного нуля сессии.
- Ротатор не участвует в отрисовке трасс (только в звуке и экспорте).
- Solo/Mute, Level и Pan — мониторинг: в `Export aligned` не попадают.
- Сетка на стенде считается от начала клипа: своей позиции в такте у файла нет.
- Колонка Role убрана из таблицы: параметр `chNN.role` жив, но им никто не
  пользуется до этапа 2.
- Возврат естественной задержки комнаты записан, но не сделан: сейчас Analyze
  выравнивает оверхеды и комнату до нуля.
- Сегментация удара записана, но не сделана: ни огибающих, ни защищённых зон,
  ни разметки для протокола проверки пока нет.
- GitHub Require PR on `main`.

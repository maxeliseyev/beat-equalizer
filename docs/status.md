# Status

Updated: 2026-09-02
Stage: 2 (редактор в standalone)
Plan step: этап 2, шаг 3 — сверка по всем микрофонам, проверка на реальном ките
Branch: `feat/real-kit-bench` (поверх `feat/cross-mic-match`, PR #27)
PR: —
Blockers: none

## Done

- Этап 1 закрыт: PR 1–8 плана, последний — прогон реального кита через стенд.
- Репо-PR 1–26 в `main`. Этап 2: шаг 1 — `beat_doc` и стенд, шаг 2 — детектор.
- PR #27 (не смержен): `CrossMicMatcher` и общий `HitSegment`.
- На этой ветке: реальный кит на стенде и протокол его задержек. 0.15.0.

## Now

`make test` — 125 кейсов. Без кита: 118 прошли, 6 пропущены, 1 упал как
ожидаемый. С китом (`BEAT_REAL_KIT_DIR=…`): 122 прошли, 3 упали как ожидаемые,
765 проверок. `make test-gui` — 51 кейс.

Звук в репозиторий не кладётся: путь берётся из окружения, `.gitignore`
закрывает аудио по расширениям, в репо попадают только числа.

Протокол кита YAN9 записан в `docs/real-kit-protocol.md`. Расстановка сходится
с физикой, разброс задержек 0.02–0.14 мс на сорока секундах игры. Комнатный
микрофон стоит на 5.03 м — за окном поиска по умолчанию (4 м), ровно как
предсказывала синтетика.

Кит сломал три вещи, и каждая записана исполняемым тестом `[!shouldfail]`:
уточнение GCC-PHAT портит априорную задержку на реальном просачивании; кадр
сверки не помещается в БПФ на 96 кГц; детектор ставит два события в один
приход.

## Next

Чинить найденное — отдельным шагом, потому что первый дефект меняет саму
оценку задержки в матчере:

1. Порядок БПФ выводить из кадра и окна поиска, а не держать константой в
   сэмплах. Правка на строку, инвариант 4.
2. Минимальный интервал держать на приходах событий, а не на кадрах потока.
3. Оценка задержки на реальном просачивании: короткий кадр вокруг прихода,
   узкий поиск вокруг априорной задержки, низкая полоса — и признать, что
   без хорошей априорной задержки уточнять нечего.

Дальше по плану — первый шаг этапа 2, который будет видно глазами: `beat_doc`
в плагине, кнопка Detect на стенде, маркеры на осциллограммах и на полосе
обзора, поле задержек таблицей.

## Resume

1. `git fetch && git checkout feat/real-kit-bench && git pull`
2. `BEAT_REAL_KIT_DIR=/path/to/kit make test` — 125 кейсов, 3 ожидаемых
   падения. Без переменной тоже зелено.
3. Читать `docs/real-kit-protocol.md`: там числами записано, что кит меряется,
   и что именно на нём разваливается.

## Open

- Кит один. Любое число протокола — свойство этой расстановки, а не барабанов
  вообще; подгонять по нему пороги нельзя.
- Априорные `d[j]` матчеру передаёт вызывающий. Связи со статическим анализом
  этапа 1, который эти числа уже считает, пока нет.
- Профиль сессии (энергетический вектор против типичного для этого удара) в
  оценку не входит: для него нужна калибровка под сессию и типы ударов —
  ступень 2 лестницы детектора, ещё не сделана.
- Калибровка сессии обязана **выбирать** удары: на плотной игре линейка
  прихода молча начинает мерить границу окна. Механизма выбора нет.
- Перцептивную атаку в неопорных каналах матчер не считает: она меряется по
  полосам, а полосы считает детектор. Приход там честный, атака равна приходу.
- Тип удара не определяется.
- Пороги детектора и матчера не калибруются под сессию.
- Замороженного набора реальных сессий нет: ступени 4–5 закрыты.
- `beat_doc` не сериализуется и не линкуется плагином.
- Клип целиком в памяти: 16 дорожек по 6 минут на 96 кГц ≈ 2.2 ГБ. Решение
  (memory-mapped источники, пик-пирамида) описано в плане, секция 8, не сделано.
- Мониторинг идёт только в выходы 1-2: кит на восемь выходов карты не разводится.
- В хосте окно Time упирается в секунду: столько держит кольцо живого входа.
- На длинном окне колонка Corr считается по прореженным точкам, то есть по
  огибающей; форму волны смотреть на коротком окне.
- Файлы обязаны быть экспортированы от одного нуля сессии.
- Ротатор не участвует в отрисовке трасс (только в звуке и экспорте).
- Solo/Mute, Level и Pan — мониторинг: в `Export aligned` не попадают.
- Сетка на стенде считается от начала клипа: своей позиции в такте у файла нет.
- Возврат задержки комнаты описан моделью, но рендером не сделан.
- GitHub Require PR on `main`.

# Status

Updated: 2026-09-02
Stage: 2 (редактор в standalone)
Plan step: этап 2, шаг 1 — модель документа и синтетический стенд
Branch: `feat/document-model`
PR: (открывается)
Blockers: PR #24 — компактная строка канала, которая не доехала до `main`

## Done

- Этап 1 закрыт: PR 1–8 плана, последний — прогон реального кита через стенд.
- Репо-PR 1–22 в `main`; #23 ушёл мимо `main` и возвращается через #24.
- На ветке: библиотека `beat_doc` рядом с `beat_dsp` и многоканальный
  синтетический дубль в тестах. 0.12.0.

## Now

Ветка зелёная: `make test` 92 кейса, 526 проверок.

`beat_doc` — источники и каналы, событие с происхождением, уверенностью и
энергетическим вектором, поканальные наблюдения (приход и перцептивная атака
порознь), поле задержек `d(событие, канал)` с возвратом `r`, кэш признаков
отдельно от решений, журнал правок с разделением «ошибка» и «так задумано»,
защищённая зона как объединение атак по всем микрофонам. JUCE библиотека не
знает; плагин её пока не линкует.

`tests/SyntheticKit.h` умеет рендерить дубль: удары с известными временами,
дробный приход в каждый микрофон, просачивание со своим расписанием, пол шума.
Проверено, что GCC-PHAT достаёт из него заложенные 3.5 и 240 сэмплов с
ошибкой < 0.1.

## Next

Этап 2, шаг 2 по `drum-editor-plan.md`, раздел 9: `IOnsetDetector` и
`SpectralFluxDetector` — ступени 1–2 лестницы (широкополосная огибающая,
6–8 логарифмических полос). Меряется на синтетике в сэмплах; precision и
recall — отдельными числами, не F1.

Отдельно и по-прежнему: записать в `docs/` протокол ожидаемых задержек по
итогам прогона на ките (OH против бочки, нижний снейр, комната). Без этих
чисел «разброс задержек сессии» на шаге 3 останется захардкоженной константой.

## Resume

1. `git fetch && git checkout feat/document-model && git pull`
2. `make test` — 92 кейса.
3. Смотреть `src/doc/Document.h`: он один описывает, из чего состоит документ.
   Дальше — `tests/test_delay_field.cpp` (возврат задержки комнаты числами) и
   `tests/test_protected_zone.cpp` (объединение атак по микрофонам).

## Open

- `beat_doc` не сериализуется: файла проекта пока нет, состояние живёт в APVTS
  плагина. Формат появится вместе с первым, что стоит сохранять, — событиями.
- Плагин `beat_doc` не линкует и не должен, пока документ ничего не решает.
- Ступени 4–5 лестницы (NMF, модель) закрыты до замороженного тестового набора.
- Клип целиком в памяти: 16 дорожек по 6 минут на 96 кГц ≈ 2.2 ГБ. Решение
  (memory-mapped источники, пик-пирамида) описано в плане, секция 8, не сделано.
- Мониторинг идёт только в выходы 1-2: кит на восемь выходов карты не разводится.
- В хосте окно Time упирается в секунду: столько держит кольцо живого входа.
- На длинном окне колонка Corr считается по прореженным точкам, то есть по
  огибающей; форму волны смотреть на коротком окне.
- Файлы обязаны быть экспортированы от одного нуля сессии.
- Ротатор не участвует в отрисовке трасс (только в звуке и экспорте).
- Solo/Mute, Level и Pan — мониторинг: в `Export aligned` не попадают.
- Сетка на стенде считается от начала клипа: своей позиции в такте у файла нет.
- Возврат задержки комнаты описан моделью, но рендером не сделан: Analyze
  по-прежнему выравнивает оверхеды и комнату до нуля.
- GitHub Require PR on `main`.

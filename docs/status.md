# Status

Updated: 2026-09-02
Stage: 2 (редактор в standalone)
Plan step: этап 2, шаг 3 — сверка по всем микрофонам, починка по реальному киту
Branch: `fix/real-kit-defects` (поверх `fix/real-kit-bench-into-main`, PR #29)
PR: —
Blockers: none

## Done

- Этап 1 закрыт: PR 1–8 плана, последний — прогон реального кита через стенд.
- Репо-PR 1–27 в `main`. Этап 2: шаг 1 — `beat_doc` и стенд, шаг 2 — детектор,
  шаг 3 — сверка по микрофонам.
- PR #29 (не смержен): перенос стенда реального кита, который #28 отправил в
  свою базу вместо `main`.
- На этой ветке: починены все четыре дефекта, найденные китом. 0.16.0.

## Now

`make test` — 125 кейсов. Без кита: 119 прошли, 6 пропущены, 702 проверки.
С китом (`BEAT_REAL_KIT_DIR=…`): все 125, 786 проверок, ни одного ожидаемого
падения — метки `[!shouldfail]` сняты, тесты стали обычными.
`make test-gui` — 51 кейс.

Отбеливание PHAT в сверке заменено обычной корреляцией: на реальном
просачивании оно стабильнее в 10–40 раз. Кадр укорочен с 85 до 20 мс, окно
уточнения — ±2 мс вокруг предсказания. Корреляция больше не ищет удар, а
только уточняет: широкий поиск вслепую даёт устойчиво неверный ответ.

Порядок БПФ выводится из кадра, а `GccPhat` не берёт кадр длиннее, чем
влезает вместе с окном поиска. Тот же корень нашёлся в `AlignmentEngine`
этапа 1 и починен там же.

Детектор больше не ставит два события в один приход: на том же отрезке 65
событий вместо 71 и ни одной пары ближе минимального интервала.

## Next

Первый шаг этапа 2, который будет видно глазами: `beat_doc` в плагине, кнопка
Detect на стенде, события в worker-потоке и маркеры на осциллограммах и на
полосе обзора. Поле задержек показывается таблицей: до `GlideRenderer` надо
своими глазами увидеть, насколько по-ударные задержки отличаются от
статической — это открытый вопрос плана, и закрывать его надо измерением.

Отдельно и раньше остального: априорная задержка. Уточнение теперь стабильно,
когда предсказание верное, а считать предсказание пока некому. Это калибровка
сессии, ступень 2 лестницы детекции.

## Resume

1. `git fetch && git checkout fix/real-kit-defects && git pull`
2. `BEAT_REAL_KIT_DIR=/path/to/kit make test` — 125 кейсов, все зелёные.
   Без переменной тоже зелено, шесть тестов пропускаются.
3. Читать `docs/real-kit-protocol.md`: там числа перебора по отбеливанию и
   раздел «что осталось открытым».

## Open

- Кит один. Любое число протокола — свойство этой расстановки, а не барабанов
  вообще; подгонять по нему пороги нельзя.
- Хорошей априорной задержки нет. Уточнение стабильно (разброс 0.03–0.06 мс на
  одиночных ударах), но грубый шаг на плотной игре даёт 0.5–2.9 мс: по
  огибающей канала в широком окне не отличить нужный удар от соседнего.
- Приход по доле от пика (`segmentHit`) несравним между каналами разной
  громкости. Для защищённой зоны это верно, для разности задержек — нет.
  Абсолютный порог над полом пробовали, на плотной игре он хуже.
- Априорные `d[j]` матчеру передаёт вызывающий. Связи со статическим анализом
  этапа 1, который эти числа уже считает, пока нет.
- Профиль сессии (энергетический вектор против типичного для этого удара) в
  оценку не входит: для него нужна калибровка под сессию и типы ударов —
  ступень 2 лестницы детектора, ещё не сделана.
- Калибровка сессии обязана **выбирать** удары: на плотной игре линейка
  прихода молча начинает мерить границу окна. Механизма выбора нет.
- Перцептивную атаку в неопорных каналах матчер не считает: она меряется по
  полосам, а полосы считает детектор. Приход там честный, атака равна приходу.
- Тип удара не определяется.
- Пороги детектора и матчера не калибруются под сессию.
- Замороженного набора реальных сессий нет: ступени 4–5 закрыты.
- `beat_doc` не сериализуется и не линкуется плагином.
- Клип целиком в памяти: 16 дорожек по 6 минут на 96 кГц ≈ 2.2 ГБ. Решение
  (memory-mapped источники, пик-пирамида) описано в плане, секция 8, не сделано.
- Мониторинг идёт только в выходы 1-2: кит на восемь выходов карты не разводится.
- В хосте окно Time упирается в секунду: столько держит кольцо живого входа.
- На длинном окне колонка Corr считается по прореженным точкам, то есть по
  огибающей; форму волны смотреть на коротком окне.
- Файлы обязаны быть экспортированы от одного нуля сессии.
- Ротатор не участвует в отрисовке трасс (только в звуке и экспорте).
- Solo/Mute, Level и Pan — мониторинг: в `Export aligned` не попадают.
- Сетка на стенде считается от начала клипа: своей позиции в такте у файла нет.
- Возврат задержки комнаты описан моделью, но рендером не сделан.
- GitHub Require PR on `main`.
